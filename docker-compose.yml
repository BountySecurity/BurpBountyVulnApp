version: '3.8'
services:
  vulnapp:
    build: .
    ports:
      - "8088:8088"
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp:size=50M
